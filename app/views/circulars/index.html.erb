<p id="notice"><%= notice %></p>

<h1>Circulars</h1>

<table>
  <thead>
    <tr>
      <th>Circular Id</th>
      <th>Circular Type</th>
      <th>From</th>
      <th>Keywords</th>
      <th>Receipt Date</th>
      <th>Broadcast Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @circulars.each do |circular| %>
      <tr>
        <td><%= circular.circularid %></td>
        <td><%= circular.circulartype %></td>
        <td><%= circular.from %></td>
        <td><%= circular.keywords %></td>
        <td><%= circular.receiptdate %></td>
        <td><%= circular.broadcastdate %></td>
        <td><%= link_to 'Show', circular %></td>
        <td><%= link_to 'Edit', edit_circular_path(circular) %></td>
        <td><%= link_to 'Destroy', circular, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Circular', new_circular_path %>
